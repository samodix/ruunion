import type { WordPressPage } from "@/types/wordpress";

// LOT 1 : remplacer ce mock par un client REST côté serveur après validation.
export async function getWordPressPages(): Promise<WordPressPage[]> {
  return [];
}
